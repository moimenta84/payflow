server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "http://localhost:5173"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
      routes:
        - id: auth-public
          uri: lb://auth-service
          predicates:
            - Path=/auth/login, /auth/register, /auth/reset-password

        - id: auth-protected
          uri: lb://auth-service
          predicates:
            - Path=/auth/me
          filters:
            - name: JwtAuthFilter

        - id: transactions
          uri: lb://transaction-service
          predicates:
            - Path=/transactions/**
          filters:
            - name: JwtAuthFilter

        - id: orders
          uri: lb://trading-service
          predicates:
            - Path=/orders/**, /portfolio/**
          filters:
            - name: JwtAuthFilter

        - id: assets
          uri: lb://trading-service
          predicates:
            - Path=/assets/**

        - id: admin
          uri: lb://auth-service
          predicates:
            - Path=/admin/**
          filters:
            - name: AdminRoleFilter

        - id: prices
          uri: lb://price-service
          predicates:
            - Path=/prices/**

        - id: alerts
          uri: lb://price-service
          predicates:
            - Path=/alerts, /alerts/**
          filters:
            - name: JwtAuthFilter

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}

app:
  jwt:
    secret: ${JWT_SECRET:payflow-super-secret-key-2024-tfg}
